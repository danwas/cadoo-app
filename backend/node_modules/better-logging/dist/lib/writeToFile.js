"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeLogToFile = void 0;
const fs_1 = __importDefault(require("fs"));
const removeColor_1 = require("./util/removeColor");
exports.writeLogToFile = (path, formattedMsg, remainingArgs) => {
    const colorlessMsg = removeColor_1.removeColors(formattedMsg);
    const joinedArgs = remainingArgs.map((arg) => {
        try {
            return String(arg);
        }
        catch (_a) {
            return `[Error: String Casting Failed]`;
        }
    }).join(' ');
    const completeLine = `${colorlessMsg} ${joinedArgs}\n`;
    fs_1.default.appendFile(path, completeLine, (err) => {
        if (!err) {
            return;
        }
        throw err;
    });
};
